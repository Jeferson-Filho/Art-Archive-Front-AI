# Comparative Analysis Prompt — Art Archive AI

**Version:** 1.0
**Date:** 2026-07-26
**Status:** In review — subject to iterative refinement (Section 5)
**Parent document:** [11 — Prompt Engineering](./11-prompt-engineering.md)
**Base documents:** [02.1 — Insight Card](../requeriments/02.1-insight-card.md), [05 — Database](../05-database.md), [09 — AI Pipeline](../09-ai-pipeline.md)

---

## 1. System Message (`v1`)

```text
You are an expert art historian and museum curator writing for a general audience
with no prior art history background. Your goal is to make art accessible,
engaging, and educational for visitors of a digital art archive.

Given an image of an artwork and its available metadata, produce a narrative
(2 to 4 short paragraphs) comparing this artwork to at least one other artist
or artwork with similar characteristics in terms of period, technique, or
culture. Be concrete about what makes them comparable (shared technique, same
movement or period, related cultural context, thematic parallels, etc.).

You may search the web for additional reliable information about the artwork,
its artist, period, technique, or culture to inform your response. You are not
restricted to any predefined list of websites or sources.

Rules:
- Base your response only on verifiable art-historical knowledge. Only
  reference a comparable artist or artwork if you are reasonably confident the
  comparison is accurate — do not invent a comparison to fill the gap.
- If you do not have enough reliable information to reference at least one
  other artist or artwork with a genuinely similar period, technique, or
  culture, return an empty string for "text" and an empty array for "sources".
- Return, in "sources", only the web pages you actually consulted that contain
  relevant information about the artwork, its artist, its period, its
  technique, or its culture. Discard any page that contains none of this. If,
  after discarding, no source remains, return an empty string for "text" and
  an empty array for "sources".
- If some metadata fields are not provided, do not mention that they are
  missing — simply write using the information available.
- Do not repeat the raw metadata verbatim; synthesize it into fluent prose.
- Write in English.
- Respond using only the provided JSON schema. Do not include any text outside
  the structured output.
```

## 2. User Message — Template (`v1`)

```text
Artwork metadata:
- Title: {title}
- Artist(s): {artists}
- Date: {date_display}
- Period: {period}
- Culture: {culture}
- Classification: {classification}
- Technique: {technique}
- Medium: {medium}

(Fields with no available value are omitted above — see ArtworkMetadata, doc 09 §3.)

Analyze the attached image together with this metadata and produce the
comparative analysis.
```

The image is attached as a second content block in the same message (`image_url`), not as text.

## 3. Output JSON Schema (`v1`)

```json
{
  "name": "comparative_analysis_content",
  "schema": {
    "type": "object",
    "properties": {
      "text": { "type": "string" },
      "sources": { "type": "array", "items": { "type": "string" } }
    },
    "required": ["text", "sources"],
    "additionalProperties": false
  }
}
```

`text` may legitimately be an empty string (RF-003, scenario 3.2; RF-010, scenario 10.2) — this is not a schema violation. `sources` must be `[]` whenever `text` is `""`.

## 4. Example Call Payload (Azure OpenAI Chat Completions)

```json
{
  "model": "gpt-4o",
  "temperature": 0.35,
  "max_tokens": 700,
  "response_format": {
    "type": "json_schema",
    "json_schema": { "...": "see Section 3" }
  },
  "messages": [
    { "role": "system", "content": "<system message — Section 1>" },
    {
      "role": "user",
      "content": [
        { "type": "text", "text": "<filled-in user message — Section 2>" },
        { "type": "image_url", "image_url": { "url": "<image_url of the artwork>" } }
      ]
    }
  ]
}
```

Follows the common call shape defined in doc 11 (§3).

## 5. Future Refinement Candidates

- **Structural reinforcement:** today the requirement of "at least one reference" (RF-003) is only a textual instruction, with no structural validation. A `v2` could change the schema to an array of objects (`{ "artist_or_work": string, "similarity": string }` with `minItems: 1`), making the requirement programmatically verifiable instead of only instruction-based.
- **Anti-hallucination instruction:** the current rule ("do not invent a comparison to fill the gap") is a first attempt; it may need explicit examples (few-shot) if the qualitative evaluation (doc 14) shows invented or inaccurate comparisons.
- **Source self-filtering reliability:** RF-010's "no appropriate source → empty text" rule currently relies entirely on the model correctly self-judging relevance within the same call. If quality evaluation (doc 14) shows the model returning sources that do not actually meet the relevance bar, this may need a stronger instruction or a worked example.

## 6. Version Log

| Version | Date       | Change                                                                                      | Reason                                                       |
| ------- | ---------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| `v1`    | 2026-07-26 | Initial independent version — own call, own schema (`text` + `sources`), web search enabled | First implementation of the per-block pipeline (doc 09 v2.0) |

> New rows must be added to this table **before** any new version becomes the active version in the pipeline (doc 11, §4).

## 7. Traceability

| Prompt Element                                              | Related Requirements | Related Documents |
| ----------------------------------------------------------- | -------------------- | ----------------- |
| `comparative_analysis` instruction (Section 1)              | RF-003               | doc 09 (§3, §5)   |
| "Omit missing fields" rule (Section 1)                      | RF-002 scenario 2    | doc 09 (§3)       |
| Empty-`text`-if-insufficient-information rule (Section 1)   | RF-003 scenario 3.2  | doc 09 (§7)       |
| Web search instruction (Section 1)                          | RF-009               | doc 09 (§6)       |
| Source self-filtering / empty-if-no-source rule (Section 1) | RF-010               | doc 09 (§7)       |
| `comparative_analysis_prompt_version` column (Section 6)    | RNF-006              | doc 05 (§5.4)     |

> The full matrix, connecting requirements to architecture components and test cases, will be formalized in document 16 — Traceability Matrix.
